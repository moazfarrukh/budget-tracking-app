import { createTheme } from "@mui/material";

export const analyticsTheme = createTheme({
    palette: {
        primary: {
            main: '#FFC107',
        },
        secondary: {
            main: '#000000',
        },
    },
});

